plugins {
    id 'java'
}

group = 'be.helha.eBar'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    implementation 'org.postgresql:postgresql:42.3.2' // Mettez à jour cette version si nécessaire
    implementation 'com.google.code.gson:gson:2.10.1'
}

test {
    useJUnitPlatform()
}